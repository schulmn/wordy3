# Server environment variables
# Use this as a template for your deployment environment variables

# Server settings
NODE_ENV=production
PORT=3000

# Database settings
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=wordy3

# CORS settings
# Use * during initial setup, then update to your frontend URL
CORS_ORIGIN=*
# For production, set to your frontend URL, e.g.:
# CORS_ORIGIN=https://wordy3-frontend.onrender.com

# No session or admin settings needed as admin functionality has been removed
